<h1>Result</h1>

<p>Score: <%=session[:score]%><p>
<%if @is_valid && @result%>
  <p><strong>Congratulations!</strong> <%=@word%> is a valid English word!</p>
<%elsif @is_valid && !@result%>
  <p>Sorry but <strong><%=@word.upcase%></strong> does not seem to be a valid English word...</p>
<%else%>
  <p>Sorry but <strong><%=@word.upcase%></strong> can't be built out of <%=@grid.join(', ')%></p>
<%end%>

<%= link_to "Play again", new_path %>
